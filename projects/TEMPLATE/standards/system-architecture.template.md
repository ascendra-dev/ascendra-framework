# System Architecture Standard Template

> **[AI Guide — Document Level]**
> `gen-architecture` writes `projects/{PROJECT_CODE}/standards/system-architecture.md` from this
> template after the PO confirms the tech stack (constraints discovery) and the architecture
> pattern (repo topology, internal layering). Per `FW-025`, there is no fixed "Ascendra standard
> stack" — every section below reflects whatever was actually confirmed for this project, derived
> from its BRD, domain, and constraints. This is the project's own record, not a copy of a
> framework-wide default.
>
> **File location:** `projects/{PROJECT_CODE}/standards/system-architecture.md`

---

# System Architecture — {PROJECT_CODE}

Generated by `/gen-architecture` on {date} from constraints discovery.

## Purpose

Defines the confirmed application stack and architectural principles for this project. Every
project command that touches Sections 2–6 of the architecture document reads this file first.
Deviations from what's recorded here require a documented decision in the project's Architecture
Document (Section 9).

---

## Confirmed Application Stack

> **[AI Guide]** One row per layer actually confirmed in scope for this project — omit rows for
> layers not in scope (e.g. no Cache row if the project has no caching need). Do not include rows
> "for completeness" that aren't part of this project's real stack.

| Layer | Technology | Version |
|-------|-----------|---------|
| Frontend | {value} | {value} |
| UI Library | {value} | {value} |
| Backend API | {value} | {value} |
| Database access | {value} | {value} |
| Database | {value} | {value} |
| Cache / Sessions / Queues | {value} | {value} |
| Language | {value} | {value} |
| Containerisation | {value} | {value} |
| CI/CD | {value} | {value} |

## Confirmed Architecture Pattern

> **[AI Guide]** From the Architecture Pattern Discovery step — repo/scaffold topology and internal
> layering depth, each with its reasoning. Both have a default; state the actual confirmed choice,
> not the default, if they differ.

| Dimension | Confirmed choice | Reason |
|-----------|------------------|--------|
| Repo topology | {value — e.g. `{code}-api` + `{code}-web`} | {value} |
| Internal layering | {value — e.g. Controller → Service} | {value} |

---

## Application Architecture

> **[AI Guide]** A simple diagram of how the confirmed pieces talk to each other — which layer
> calls which, over what protocol, and which layer owns the database. Adapt the shape to the
> confirmed stack; do not force a two-tier web+API diagram onto a project with a different
> topology (e.g. a single monolith, or a project with a mobile app added).

```
[Diagram reflecting the confirmed repo topology and how each piece communicates]
```

[1–2 short paragraphs describing the runtime relationship between the confirmed repos/services —
what talks to what, over what protocol, who owns the database.]

---

## Layer Responsibilities

> **[AI Guide]** One subsection per confirmed repo/service. State what belongs in each layer and
> what explicitly does not — this is what prevents logic ending up in the wrong place later.

### {Repo/Service Name}
- {Responsibility}
- {Responsibility}
- {What does NOT belong here}

[Repeat per confirmed repo/service]

### Internal Layers

> **[AI Guide]** State the confirmed internal layering pattern from the table above, explicitly —
> what each layer is responsible for. If the default (Controller → Service, no repository class)
> was confirmed, say so plainly; if a fuller layered pattern was confirmed instead, define each
> layer's responsibility here.

| Layer | Responsibility |
|-------|---------------|
| {Layer} | {Responsibility} |

---

## Core Architectural Principles

> **[AI Guide]** Numbered, non-negotiable principles for this project — derived from the confirmed
> stack and the project's own constraints (multi-tenancy shape, compliance signals). Universal
> principles that apply regardless of stack (API-first, validate at the boundary, stateless
> services) still belong here if they're actually true for this project — restate them in terms of
> the confirmed technologies, don't just reference a generic framework default.

### 1. {Principle name}
{What it means and why it's non-negotiable for this project.}

[Repeat, numbered, for every principle that governs this project]

---

## What Requires a Documented Architectural Decision

> **[AI Guide]** Things that are not covered by the confirmed stack and must be explicitly decided
> and recorded in the project's Architecture Document (Section 8 or 9) rather than self-approved.

- Replacing any confirmed stack component
- Introducing a technology not in the confirmed stack
- {Project-specific escalation trigger}

If a requirement cannot be implemented within the confirmed stack, escalate — do not self-approve
a new technology.

---

## Data Localisation

> **[AI Guide]** Only if the project has a regulatory data-residency constraint (from constraints
> discovery / domain compliance signals). If none, write "Not applicable — no data residency
> constraint identified for this project."

[Hosting region and justification, or "Not applicable."]

---

## What Must Never Happen

> **[AI Guide]** The concrete, specific failure modes this project's principles above are meant to
> prevent — stated as a checklist so a reviewer can verify against it directly.

- {Concrete prohibited action}
- {Concrete prohibited action}
