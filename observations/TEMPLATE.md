# OBS-XXX — [Short Title]

> **[AI Guide]** Title format is `OBS-XXX — [Short noun phrase describing what was observed]`. Use the next available number from `observations/index.md`. This is a raw finding, not a ratified decision — keep it light. Do not add sections beyond what's below; a finding worth more structure than this is probably ready to be proposed directly as a `decisions/FW-XXX` draft instead of an observation.
>
> Delete this guide block before writing the file.

| Field | Value |
|-------|-------|
| **ID** | OBS-XXX |
| **Date** | YYYY-MM-DD |
| **Status** | New |
| **Area** | [Which command(s), template(s), or framework concept this touches] |
| **Found while** | [What was actually running — e.g. `/anchor-project` driving `/gen-brd` for PROJECT-CODE, or standalone `/review-stories`] |

> Status values: `New` (default) · `Under Review` · `Promoted → FW-XXX` · `Rejected` · `Won't Fix`

---

## What was observed

[1–3 sentences. The concrete friction: what didn't work as documented, what was genuinely ambiguous, or what real improvement opportunity surfaced. Cite the specific file/section/command involved, not a vague impression.]

---

## Why it matters

[1–2 sentences. What breaks, or what gets harder, because of this — for this project specifically, or for the framework generally if left unaddressed.]

---

## Proposed direction

[Optional. 1–2 sentences if a fix is obvious; "Not clear — needs the framework author's judgment" if it isn't. Never a full solution design — that's what a promoted `FW-XXX` decision is for.]
